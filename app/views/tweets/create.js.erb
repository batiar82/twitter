<% if @tweet.new_record? %>
    $('textarea#tweet_post').effect('highlight', {color: 'red' });
<%else%>
    $("div#tweets").append("<%= escape_javascript(render @tweet) %>");
    $("div.<%=dom_id(@tweet) %>").effect('highlight');
<%end%>