<% if @tweet.new_record? %>
    $('textarea#tweet_post').effect('highlight', {color: 'red' });
<%else%>
    $("div#tweets").prepend("<%= escape_javascript(render @tweet) %>");
    $("div.<%=dom_id(@tweet) %>").effect('highlight');
     $('textarea#tweet_post').val('');
<%end%>